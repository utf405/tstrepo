# Dockerfile должен находится в корневой директории приложения

# Базовый образ, приложение не работает с 3 версией Python и актуальными пакетами из requirements.txt
FROM python:2

# Рабочая директория в контейнере
WORKDIR /usr/src/app

# Копируем дистрибутив приложения в контейнер
COPY . .

# Устанавливаем требуемые пакеты
RUN pip install --no-cache-dir -r requirements.txt

# Порт, который будет слушать контейнер, по умолчанию во flask 5000, в конфиге приложения не переопределено
EXPOSE 5000

# Запускаем приложение
CMD [ "python", "app.py" ]
